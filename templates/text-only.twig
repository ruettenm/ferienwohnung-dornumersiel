{% extends "_layout/base-layout.twig" %}

{% block content %}
    {% cache %}
        <section class="section">
            <div class="container">
                <h1 class="title">{{ entry.title }}</h1>
                <div class="block">
                    <div class="content">
                        {{ entry.text }}
                    </div>
                </div>
            </div>
        </section>
    {% endcache %}
{% endblock %}
